sudo: required

services:
  - docker

before_install:
  - mv docker-compose.prod.yml docker-compose.yml
  - docker-compose up --force-recreate --no-start

before_script:
  - docker-compose run composer install -d=/var/www

script:
  - docker exec -it zolletta-slack-url-keeper-php vendor/bin/phpunit

language: php

php:
  - 7.4
